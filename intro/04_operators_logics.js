console.log("operadores lógicos");

// AND -> &&
const isThereCar = true;
const isThereMotorcycle = false;
const response = isThereCar && isThereMotorcycle;
console.log(`${isThereCar} && ${isThereMotorcycle} -> ${response}`);

// OR -> ||
const responseB = isThereCar || isThereMotorcycle;
console.log(`${isThereCar} || ${isThereMotorcycle} -> ${responseB}`);

// NOT -> !

console.log(10 >= 9);
console.log(0 == 0);
